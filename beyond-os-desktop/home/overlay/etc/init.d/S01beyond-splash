#!/bin/sh
# Keep diagnostics available on machines without a framebuffer.
case "${1:-}" in
  start) /usr/bin/beyond-splash /usr/share/beyond-home/boot.ppm || true ;;
  stop|restart|reload) ;;
  *) echo "Usage: $0 {start|stop|restart|reload}" >&2; exit 1 ;;
esac
