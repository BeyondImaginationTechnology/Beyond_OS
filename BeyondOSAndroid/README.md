# Beyond OS Android 0.1

The native companion and launcher prototype for the AOSP community track. Open in Android Studio, install API 37, then build an APK. It is intentionally a conventional Android app; it validates the command-center flow before that flow is integrated into a community-maintained AOSP device build.
