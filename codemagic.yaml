workflows:
  daily-breath-keystore-bootstrap:
    name: Daily Breath Keystore Bootstrap
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      groups:
        - daily-breath-keystore-bootstrap
      vars:
        BOOTSTRAP_KEY_ALIAS: daily-breath-upload
    scripts:
      - name: Generate upload keystore
        script: |
          set -euo pipefail
          : "${BOOTSTRAP_KEYSTORE_PASSWORD:?Set BOOTSTRAP_KEYSTORE_PASSWORD as a Codemagic secret}"
          : "${BOOTSTRAP_KEY_PASSWORD:?Set BOOTSTRAP_KEY_PASSWORD as a Codemagic secret}"
          keytool -genkeypair -v \
            -keystore "$CM_BUILD_DIR/daily-breath-upload.jks" \
            -storetype JKS \
            -storepass "$BOOTSTRAP_KEYSTORE_PASSWORD" \
            -keypass "$BOOTSTRAP_KEY_PASSWORD" \
            -alias "$BOOTSTRAP_KEY_ALIAS" \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=Daily Breath, OU=Mobile, O=Beyond Imagination, L=Seattle, ST=Washington, C=US"
    artifacts:
      - daily-breath-upload.jks

  daily-breath-android-play:
    name: Daily Breath Android Google Play
    max_build_duration: 60
    instance_type: linux_x2
    working_directory: DailyBreathAndroid
    environment:
      android_signing:
        - daily-breath-play-upload
      vars:
        ANDROID_VERSION_CODE: 3
        ANDROID_VERSION_NAME: 2.1
    scripts:
      - name: Set Android SDK location
        script: |
          set -euo pipefail
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/DailyBreathAndroid/local.properties"
      - name: Build signed Google Play App Bundle
        script: |
          set -euo pipefail
          sh ./gradlew --no-daemon \
            --console=plain \
            -PversionCode="$ANDROID_VERSION_CODE" \
            -PversionName="$ANDROID_VERSION_NAME" \
            bundlePlayRelease
    artifacts:
      - app/build/outputs/bundle/playRelease/*.aab
      - app/build/outputs/mapping/playRelease/mapping.txt
  daily-breath-android-debug:
    name: Daily Breath Android Debug APK
    max_build_duration: 45
    instance_type: linux_x2
    working_directory: DailyBreathAndroid
    scripts:
      - name: Set Android SDK location
        script: |
          set -euo pipefail
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/DailyBreathAndroid/local.properties"
      - name: Build installable debug APK
        script: |
          set -euo pipefail
          ./gradlew --no-daemon -PversionCode=6 -PversionName=2.1 assemblePlayDebug
    artifacts:
      - app/build/outputs/apk/play/debug/*.apk

  jaguar-ios-testflight:
    name: Jaguar iOS TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m2
    working_directory: JaguarApple
    integrations:
      app_store_connect: beyond-apple-api
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: technology.co.beyondimagination.jaguar
      xcode: latest
    scripts:
      - name: Generate Xcode project
        script: |
          set -euo pipefail
          command -v xcodegen >/dev/null || brew install xcodegen
          xcodegen generate
      - name: Configure signing profiles
        script: xcode-project use-profiles
      - name: Set a unique App Store build number
        script: |
          set -euo pipefail
          agvtool new-version -all "$(date +%s)"
      - name: Build signed IPA
        script: |
          set -euo pipefail
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/JaguarApple/Jaguar.xcodeproj" \
            --scheme Jaguar
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration

  jaguar-ios:
    name: Jaguar iOS checks
    max_build_duration: 60
    instance_type: mac_mini_m2
    working_directory: JaguarApple
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    environment:
      xcode: latest
    scripts:
      - name: Generate Xcode project
        script: |
          set -euo pipefail
          command -v xcodegen >/dev/null || brew install xcodegen
          xcodegen generate
      - name: Run unit tests
        script: |
          set -euo pipefail
          simulator_id="$(xcrun simctl list devices available -j | ruby -rjson -e '
            devices = JSON.parse(STDIN.read).fetch("devices").values.flatten
            device = devices.find { |candidate| candidate["isAvailable"] && candidate["name"] == "iPhone 16 Pro Max" }
            device ||= devices.find { |candidate| candidate["isAvailable"] && candidate["name"].start_with?("iPhone") }
            abort "No available iPhone simulator found" unless device
            puts device.fetch("udid")
          ')"
          xcodebuild test \
            -project Jaguar.xcodeproj \
            -scheme Jaguar \
            -destination "id=$simulator_id" \
            CODE_SIGNING_ALLOWED=NO
      - name: Build simulator app
        script: |
          set -euo pipefail
          xcodebuild build \
            -project Jaguar.xcodeproj \
            -scheme Jaguar \
            -sdk iphonesimulator \
            -derivedDataPath "$CM_BUILD_DIR/derived-data" \
            CODE_SIGNING_ALLOWED=NO
    artifacts:
      - $CM_BUILD_DIR/derived-data/Build/Products/**/*.app
      - /tmp/xcodebuild_logs/*.log

  daily-breath-testflight:
    name: Daily Breath iOS TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m2
    working_directory: DailyBreathApple
    integrations:
      # Create this named App Store Connect integration in Codemagic Team settings.
      app_store_connect: beyond-apple-api
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: technology.co.beyondimagination.thedailybreath
      xcode: latest
    scripts:
      - name: Configure signing profiles
        script: xcode-project use-profiles
      - name: Set a unique App Store build number
        script: |
          set -euo pipefail
          agvtool new-version -all "$(date +%s)"
      - name: Build signed IPA
        script: |
          set -euo pipefail
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/DailyBreathApple/TheDailyBreath.xcodeproj" \
            --scheme "The Daily Breath"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true

  daily-breath-ios:
    name: Daily Breath iOS checks
    max_build_duration: 60
    instance_type: mac_mini_m2
    working_directory: DailyBreathApple
    environment:
      xcode: latest
    scripts:
      - name: Run unit tests
        script: |
          set -euo pipefail
          simulator_id="$(xcrun simctl list devices available -j | ruby -rjson -e '
            devices = JSON.parse(STDIN.read).fetch("devices").values.flatten
            device = devices.find { |candidate| candidate["isAvailable"] && candidate["name"] == "iPhone 16 Pro Max" }
            device ||= devices.find { |candidate| candidate["isAvailable"] && candidate["name"].start_with?("iPhone") }
            abort "No available iPhone simulator found" unless device
            puts device.fetch("udid")
          ')"
          xcodebuild test \
            -project TheDailyBreath.xcodeproj \
            -scheme "The Daily Breath" \
            -destination "id=$simulator_id" \
            CODE_SIGNING_ALLOWED=NO
      - name: Build simulator app
        script: |
          set -euo pipefail
          xcodebuild build \
            -project TheDailyBreath.xcodeproj \
            -scheme "The Daily Breath" \
            -sdk iphonesimulator \
            -derivedDataPath "$CM_BUILD_DIR/derived-data" \
            CODE_SIGNING_ALLOWED=NO
    artifacts:
      - $CM_BUILD_DIR/derived-data/Build/Products/**/*.app

  beyond-baby-names-ios:
    name: Beyond Baby Names iOS checks
    max_build_duration: 60
    instance_type: mac_mini_m2
    working_directory: BeyondBabyNamesApple
    environment:
      xcode: latest
    scripts:
      - name: Run unit tests
        script: |
          set -euo pipefail
          simulator_id="$(xcrun simctl list devices available -j | ruby -rjson -e '
            devices = JSON.parse(STDIN.read).fetch("devices").values.flatten
            device = devices.find { |candidate| candidate["isAvailable"] && candidate["name"] == "iPhone 16 Pro Max" }
            device ||= devices.find { |candidate| candidate["isAvailable"] && candidate["name"].start_with?("iPhone") }
            abort "No available iPhone simulator found" unless device
            puts device.fetch("udid")
          ')"
          xcodebuild test \
            -project BeyondBabyNames.xcodeproj \
            -scheme BeyondBabyNames \
            -destination "id=$simulator_id" \
            CODE_SIGNING_ALLOWED=NO
      - name: Build simulator app
        script: |
          set -euo pipefail
          xcodebuild build \
            -project BeyondBabyNames.xcodeproj \
            -scheme BeyondBabyNames \
            -sdk iphonesimulator \
            -derivedDataPath "$CM_BUILD_DIR/derived-data" \
            CODE_SIGNING_ALLOWED=NO
    artifacts:
      - $CM_BUILD_DIR/derived-data/Build/Products/**/*.app

  beyond-french-ios-testflight:
    name: Beyond French iOS TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m2
    working_directory: BeyondFrenchApple
    integrations:
      app_store_connect: beyond-apple-api
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: technology.co.beyondimagination.beyondfrench
      xcode: latest
    scripts:
      - name: Generate Xcode project
        script: |
          set -euo pipefail
          command -v xcodegen >/dev/null || brew install xcodegen
          xcodegen generate
      - name: Configure signing profiles
        script: xcode-project use-profiles
      - name: Set a unique App Store build number
        script: |
          set -euo pipefail
          agvtool new-version -all "$(date +%s)"
      - name: Build signed IPA
        script: |
          set -euo pipefail
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/BeyondFrenchApple/BeyondFrench.xcodeproj" \
            --scheme BeyondFrench
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
